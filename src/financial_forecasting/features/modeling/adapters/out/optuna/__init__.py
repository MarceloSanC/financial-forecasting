"""Adapter da busca de hiperparâmetros — única casa de `optuna` no projeto.

O gate import-linter `modeling-no-optuna-leak` reprova o import da lib em
`modeling.application` ou `modeling.domain`.
"""
